{
  "scorecard_schema_version": 1,
  "description": "Frozen trace-scorecard v1 contract for the agent-delivery-harness (issue #104). Machine-readable authority for the cross-run scorecard emitted by scripts/trace-scorecard.sh to <main root>/tests/evals/scorecards/trace-scorecard.json (single stable filename, idempotent whole-file overwrite, never append, no generation timestamp — reruns over unchanged inputs are byte-identical). Consumes the per-issue trace-summary v1 objects (trace-summary.v1.json) plus one sanctioned peek at a sibling trace.jsonl for version attribution. Sibling of trace-summary.v1.json, same documentation style.",
  "notes": {
    "absence_semantics": "null means no data; 0 means measured zero; [] means the detector/collector ran and found nothing. A bucket where no run carried token data emits tokens null (never a fabricated {input: 0, output: 0}); token_coverage {runs_with_tokens, of} carries the honest denominator. Same-class coverage counts measured inputs over all relevant aggregated inputs; old summaries without the projection remain null on issue rows and do not add an invented zero. Rates always carry an explicit of/coverage denominator so absent data can never dilute a rate.",
    "open_world": "Additive top-level keys are allowed in later v1.x revisions; existing keys and their semantics are frozen. Consumers reject unknown majors of scorecard_schema_version. The same rule is applied upstream: an input summary with an unknown major of summary_schema_version is skipped untouched (listed under inputs.skipped), never interpreted under the trace-summary v1 contract.",
    "single_source_of_numbers": "The markdown report on stdout is rendered FROM this object, so the human report and this machine scorecard can never disagree.",
    "computed_only": "Every value is computed from the trace-summary v1 objects on disk (plus the sanctioned attribution peek at trace.jsonl); nothing is self-reported and nothing is regenerated or repaired. Missing summaries are reported under inputs.missing_summaries with the trace-report.sh regeneration hint; unreadable or unknown-major summaries under inputs.skipped.",
    "version_attribution": "A run whose summary carries exactly one harness_versions element is attributed to it (attribution \"single\"). A multi-version run with a readable sibling trace.jsonl is attributed to the LAST version-carrying span's harness.version (attribution \"last_seen_in_trace\") — never the summary's sort order, which is meaningless for git SHAs, and never simply the last line. Unattributable runs are never guessed: a multi-version run without a readable trace, or a run that recorded no harness.version at all (harness_versions []), lands in the visible synthetic \"mixed\" bucket (attribution \"unresolved_mixed\") with the full harness_versions list preserved on the run row. A summary whose harness_versions is not an array is malformed and is skipped-with-note (inputs.skipped), never routed into attribution.",
    "deferred_metrics": "Per-feature tool-call attribution remains DEFERRED because trace-summary v1 tools[] has no feature dimension. It is never fabricated from lifecycle edge timing. Issue #296 resolved the former review-verdict and per-feature elapsed deferrals through optional additive trace-summary projections.",
    "issue_62_mapping": "Mapping to #62 (L0/L1 local eval runner) is documented, not forked: by_version[].issues[] rows are RUN AGGREGATES over real issue runs, not graded eval cases — a case-level eval scorecard (manifest/fixture/label/prediction rows) is a different artifact, and reusing its row shape here would force fake label/prediction fields. This scorecard shares #62's top-level conventions where they genuinely apply (a schema-version field, runtime, a rows+aggregates split). If/when #62 lands, its runner can either adopt this run-row shape for trace aggregation or map each by_version[].issues[] row into its own results[] with boundary \"trace-aggregate\". The version field is the seam: consumers reject unknown majors; additive keys are allowed in 1.x.",
    "visibility": "Local-only, never committed; covered by the .gitignore rule pair tests/evals/scorecards/* + !tests/evals/scorecards/.gitkeep (generated scorecards are local artifacts per the l0 eval spec)."
  },
  "required_top_level": [
    "scorecard_schema_version",
    "generator",
    "runtime",
    "source_root",
    "summary_schema_versions_seen",
    "inputs",
    "by_version",
    "compat"
  ],
  "bucket_fields": [
    "harness_version",
    "runs",
    "finished",
    "passed",
    "red_reentry_free_rate",
    "deviations",
    "tool_calls",
    "tokens",
    "token_coverage",
    "tool_coverage",
    "feature_delivery",
    "review_fail",
    "blocked_green",
    "same_class_failures",
    "skills",
    "issues"
  ],
  "fields": {
    "scorecard_schema_version": "number; always 1 under this contract.",
    "generator": "string; the emitting script, scripts/trace-scorecard.sh.",
    "runtime": "string; \"local\" for this generator (mirrors the #62 runtime convention).",
    "source_root": "string; the <main root>/.copilot-tracking/issues directory the summaries were scanned from.",
    "summary_schema_versions_seen": "array of unique numbers; every summary_schema_version seen on the AGGREGATED summaries (skipped files contribute nothing).",
    "inputs": "object {summaries_found, missing_summaries, skipped}. summaries_found counts AGGREGATED summaries only. missing_summaries lists dirs with a trace.jsonl but no trace-summary.json as {issue_dir, trace_file, hint} — hint is the trace-report.sh regeneration command (reported, never repaired). skipped lists summaries excluded from every aggregate as {summary_file, reason} (malformed JSON, or an unknown major of summary_schema_version).",
    "by_version": "array; one bucket per attributed harness version (plus the synthetic \"mixed\" bucket when unresolved multi-version runs exist), sorted by version key. Every aggregated run lands in exactly one bucket.",
    "by_version[].harness_version": "string; the attributed harness.version, or \"mixed\" for unattributable runs (multi-version with no readable trace to peek, or no harness.version recorded at all).",
    "by_version[].runs": "number; runs aggregated into the bucket.",
    "by_version[].finished": "number; runs with finished == true.",
    "by_version[].passed": "number; runs with final_outcome == \"pass\".",
    "by_version[].red_reentry_free_rate": "object {free, of}; free counts finished, passing runs whose red_reentry is [] — this measures no-red-after-green re-entry, NOT literally first-pass green (a red before a feature's first green is invisible to trace-summary v1); of is the bucket's run count (explicit denominator).",
    "by_version[].deviations": "object {count, feature_ids}; count sums deviations.count across the bucket's runs (measured zero is a real 0); feature_ids is the unique union.",
    "by_version[].tool_calls": "object {calls, fail_calls}; sums over the runs' tools[] entries.",
    "by_version[].tokens": "object {input, output} summed over ONLY the runs whose summary carries a non-null tokens object, or null when NO run in the bucket carried token data (absence is null, never 0; a tokens-null run is never averaged in as 0). Input token buckets are accepted under either key pair — {input, output} or the trace-report.sh producer's {input_tokens, output_tokens} — and always emitted here as {input, output}.",
    "by_version[].token_coverage": "object {runs_with_tokens, of}; the honest denominator for the tokens sums.",
    "by_version[].tool_coverage": "object {runs_with_tool_spans, of}; the honest denominator for tool_calls, mirroring token_coverage (issue #131). runs_with_tool_spans counts runs whose summary coverage.has_tool_spans is true; of is the bucket run count. A lifecycle-only run (no runtime adapter, no tool spans) is counted in of but not in runs_with_tool_spans, so a low tool_calls sum reads as 'the adapter was not wired' rather than 'the agent called no tools' — instrumented and lifecycle-only runs are never blended. Additive open-world optional key; absent on scorecards generated before #131.",
    "by_version[].feature_delivery": "object {samples, median_seconds, p75_seconds, p95_seconds, coverage}. samples counts paired elapsed rows from summaries carrying optional feature_delivery data. samples, distribution values, and coverage are null when no summary in the bucket carries feature_delivery. For an instrumented bucket with no pairs, samples is the measured 0 while distribution values remain null and coverage identifies the started-feature gap. Percentiles use linear interpolation; old summaries never add invented samples or denominators.",
    "by_version[].review_fail": "object {fail, of, rate}. Values sum observed review_verdicts from summaries carrying that optional projection. rate is null when of is 0; all three values are null when no summary in the bucket carries review_verdicts. Old summaries never dilute the denominator.",
    "by_version[].blocked_green": "object {blocked, of, rate}. Values sum observed green_handbacks from summaries carrying that optional projection. rate is null when of is 0; all three values are null when no summary in the bucket carries green_handbacks. This metric is diagnostic and is not an independent adoption gate.",
    "by_version[].same_class_failures": "object {occurrences_by_class, max_observed_per_run, coverage, target}. occurrences_by_class sums measured summary same_class_failures.by_class rows in closed failure-class order, or null when no summary in the bucket carries the projection. max_observed_per_run is the largest measured summary max_count, or null when unmeasured. coverage is {measured_inputs, total_relevant_inputs}, where every aggregated run is relevant but only summaries carrying the projection are measured. target is the report-only policy {operator:\"<=\", max_count:2, policy:\"report-only\"}; it is diagnostic and never a lifecycle or merge gate.",
    "by_version[].skills": "array of {name, calls, fail_calls} keyed on skill name (issue #139), summed over the bucket's runs' summary.skills; [] when no run recorded a skill. Surfaces which skills the runs invoked (loaded) per harness version. Additive open-world optional key.",
    "by_version[].issues": "array of per-run source rows. In addition to the established issue, provenance, attribution, outcome, deviation, tool, wall-clock, token, coverage, skill, loop, and invalid-line fields, each row propagates feature_delivery, review_verdicts, green_handbacks, and same_class_failures from its summary or null when an older summary lacks the optional projection. Null preserves the coverage gap; it is never converted to an empty measurement.",
    "compat": "object {issue_62_mapping}; the #62 mapping statement (see notes.issue_62_mapping)."
  }
}
