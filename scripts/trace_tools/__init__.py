"""trace_tools — Python pilot for the harness deep-trace span/log exporters.

Phase 1 is a pure scaffold: the package is importable and exposes a minimal CLI
arg router so the existing Python profile gates activate. No mapping logic is
wired to ``trace-export.sh`` yet.
"""

__version__ = "0.1.0"
